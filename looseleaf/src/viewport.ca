
type Viewport;

def create_window(string scriptFile) -> Viewport
def Viewport.resize(w, Point size)

-- These calls are available for scripts running inside a Viewport
namespace viewport
    def size() -> Point
    def mouse() -> Point



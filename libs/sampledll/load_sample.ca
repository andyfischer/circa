
patch_with_dll(reflect:this_branch(), 'sampledll' -> rpath)

def sample_a() -> string
    return 'Not patched'

namespace ns
    def concat(string a, string b) -> string
        return 'Not patched'
        
trace(sample_a())

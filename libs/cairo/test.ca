
require cairo

surface = cairo:make_image_surface([200 200])

context = surface.make_context

context.set_source_color(#f00)
context.move_to([10 10])
context.line_to([190 190])
context.close_path()
context.paint

surface.write_to_png('test_output.png')

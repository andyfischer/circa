
screen = SDL_Surface()
background = load-image('background.bmp')

function redraw()
    apply-surface(screen, background, 0, 0)

    mouse_x = 3.0
    mouse_y = 5.0
    rect_size = 2

    rectangle(screen, mouse_x - 1, mouse_y - 1, mouse_x + 1, mouse_y + 1)
end

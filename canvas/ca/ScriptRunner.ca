
msg = input()

state Branch branch


if msg[0] == 'loadFile'
    print('loading file: ' msg[1])
    branch = load_module(msg[1])
    branch.dump
elif msg[0] == 'call'

    if not(branch.is_null)
        print('branch version: ' branch.version)
        branch.call([])

else
    print("ScriptRunner didn't recognize: " msg)

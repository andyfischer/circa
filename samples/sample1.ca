
--screen = SDL_Surface()
background = load_image('background.bmp')

def redraw()
    apply_surface(screen, background, 0, 0)

    mouse_x = 3.0
    mouse_y = 5.0
    rect_size = 5

    rectangle(screen, mouse_x - rect_size, mouse_y - rect_size, mouse_x + rect_size, mouse_y + rect_size, 0)
end

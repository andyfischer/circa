

from Circa.common import function_builder
from Circa.core import builtins

"""
class CreateNew(object):
    name = 'create-new'
    inputs = ['type']
    output = 'any'

    @staticmethod
    def initialize(term):
        term.cachedValue = term.input(0).cachedValue.allocateData()


def createTerms(codeUnit):
    function_builder.importPythonFunction(codeUnit, CreateNew)
"""

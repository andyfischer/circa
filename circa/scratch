import pdb
from utils.spy_object import SpyObject
from builder import Builder
from circa_module import CircaModule
import parser
import utils.spy as spy

mod = CircaModule()
bldr = Builder(mod)
spy.printAllCalls(bldr)


parser.parse(bldr, "a=1")

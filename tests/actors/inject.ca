
square = {
    i = input()

    state injected

    output(concat('injected: ' injected))
}

actor = make_actor(square.block)

actor.inject('injected', 'a')
assert(actor.call([null]) == 'injected: a')
assert(actor.call([null]) == 'injected: a')
actor.inject('injected', 'b')
assert(actor.call([null]) == 'injected: b')

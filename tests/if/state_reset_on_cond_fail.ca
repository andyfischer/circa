
def f(bool b)
    if b
        state int i = 0
        i += 1

vm = make_vm(f)
vm.call(true)
print(vm.get_state)
vm.call(true)
print(vm.get_state)
vm.call(false)
print(vm.get_state)
vm.call(true)
print(vm.get_state)


def Block.list_statements(self) -> List
  for term in self.terms
    if not term.is_statement or term.is_hidden or term.is_comment
      discard
    term

def main()
  a = 1 + 2 + 3
  b = a * 4

  5

  for i in [0 1] {}

  main(a b)

print('statements:')
for term in main.block.list_statements
  print(' ' cond(term.has_name, str(term.name ' = '), '') term.function.name '(...)')


def list_statements(Block b) -> List
  for term in b.terms
    if not term.is_statement or term.is_hidden or term.is_comment
      discard
    term

def main()
  a = 1 + 2 + 3
  b = a * 4

  5

  for i in [0 1] {}

  main(a b)

print('statements:')
for term in list_statements(main.block)
  print(' ' cond(term.has_name, str(term.name ' = '), '') term.function.name '(...)')

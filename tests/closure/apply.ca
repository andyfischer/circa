
f = {
    x = input() + input()
    x
}

assert(f.apply([1 1]) == 2)
assert(f.apply([1 3]) == 4)

f = {
    output(1)
    output(2)
    output(3)

    :last
}

a, b, c, d = f.apply([])
assert(a == :last)
assert([b c d] == [3 2 1])


-- Trial and error shows that circa will segfault after ~5000 recursive calls
trial_count = 1000

recr_count = 1000

def f(int i)
    if i >= recr_count
        return
    else
        f(i + 1)

for i in 0..trial_count
    f(0)


-- This once caused a runtime error: Couldn't cast value 5 to type null
-- The reason is that, inside the for-loop there was an input 'non_zero',
-- which had declared type 'null', even though it wasn't necessarily null.

non_zero = null

for i in [5 0]
    if i != 0
        non_zero = i


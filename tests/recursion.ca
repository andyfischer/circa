
def fib(int i) -> int
    if i == 0 or i == 1
        return 1

    return fib(i - 1) + fib(i - 2)

def main()
  for i in 0..11
    print('fib(' i ') = ' fib(i))

vm = make_vm(main)
vm.precompile(main.block)
vm.precompile(fib.block)

require bytecode_analysis
bytecode_analysis.dump(vm)



type Point { number x, number y }

-- try annotate_type() function
l = [1.0,2.0]
l_anno = annotate_type(l,Point)
typeof(l_anno) -> print

print(l_anno.x ',' l_anno.y)

-- now try : operator, which should have the same effect
b = [5.0 3.0] : Point
print(b.x ',' b.y)

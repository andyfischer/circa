
def recr(int depth)
    print('reached depth = ' depth)

    if true
        state s = depth
        s = s * 2

    if depth == 0
        return

    for i in [0]
        state t = depth
        recr(depth - 1)
        t = t * 2

int = make_vm(recr)
int.call(3)
print('after 1st iter: ' int.extract_state)
int.call(3)
print('after 2nd iter: ' int.extract_state)
int.call(3)
print('after 3rd iter: ' int.extract_state)

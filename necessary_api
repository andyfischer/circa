

CodeUnit:
  getTerm(function, inputs, force_create?) -> term
  evaluateTerm(term)
  bindName(term, string)
  getType(term) -> term
  asString(term) -> string
  asInt(term) -> int
  setString(term, string)
  setInt(term, int)

